{
  "name": "fitzy-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev:consumer": "npm run dev --workspace consumer",
    "dev:venue": "npm run dev --workspace venue-admin",
    "dev:owner": "npm run dev --workspace owner-admin"
  }
}
